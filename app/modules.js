let modulesEnabled = {
	awy_core: {
		enabled: true,
	},
	distro_install: {
		enabled: true,
	},
	awy_admin: {
		enabled: true,
	},
	distro_frontend: {
		enabled: true,
	},
	change_frontend: {
		enabled: true,
	}
}

export default modulesEnabled;